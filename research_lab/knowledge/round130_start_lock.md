# Round 130 Start Lock

**Active round:** 130  
**Previous completed round:** 129  
**RH status:** OPEN

## Numbering lock

Round 130 is the active execution round.

Any citation of Round 99, Round 103, Round 126, Round 128, or any other lower-numbered round is a **historical dependency citation only**. It must never be described as the current round or as a rollback of the chronology.

GitHub persistence and active project chronology are currently not synchronized: the audited `research-lab-agents` branch persists source runs only through Round 88. See `research_lab/round_registry.json` and `research_lab/knowledge/round_chronology_audit_2026-08-17.md`.

## Frozen input from Round 129

Accept as exact/internal:

1. Exact centered Riemann–Siegel two-component extraction.
2. Exact Delta cancellation in the centered endpoint component.
3. Explicit one-term cutoff bridge.
4. Exact centered NC algebra.
5. Feasible-segment minimization using the real inner product for a real phase parameter.
6. Endpoint generic/microscopic scale stratification.

Do not promote as proved:

1. Full physical second-order core jet.
2. Generic transversality from F'(p) alone.
3. Full-core scale from saddle-multiplier point evaluation alone.
4. Physical repair from D_*<0 alone.
5. Rank-one structure from O'Sullivan symmetry alone.

## Frozen representation

\[
\mathbf Y_{\rm RS}
=
\begin{pmatrix}
\mathscr R_t\\
\widetilde{\mathscr M}_t
\end{pmatrix}
-
\begin{pmatrix}
\mathcal E_0\\
\widetilde{\mathcal E}_1
\end{pmatrix}
+
\begin{pmatrix}
\mathcal B_0\\
\widetilde{\mathcal B}_1
\end{pmatrix}.
\]

The map to the NC variables is real-linear:

\[
\begin{pmatrix}
T\\
\widetilde W
\end{pmatrix}
=
e^{i\phi}\,\overline{\mathbf Y_{\rm RS}}.
\]

The exact centered derivative identity is

\[
T_x=c_*T+\frac{\mu}{t}\widetilde W,
\qquad
c_*=c+\frac{\mu}{t}\Delta_*=i\phi_x-\ell\mu.
\]

## Round 130 target

\[
\boxed{
\textbf{NON-TAUTOLOGICAL CENTERED PHYSICAL JET RECONSTRUCTION}
}
\]

The core must not be expanded by a contour shift that merely crosses the integer poles, recreates the Dirichlet block through residues, and then repackages the original identity. Any acceptable core jet must either:

- cancel the residue direction before asymptotic expansion; or
- extract only the physical NC projections through an independent completed/asymmetric identity.

## Required output of Round 130

Construct or refute a rigorous projected model of the form

\[
\mathbf P_{\rm NC}
=
\mathbf M_{\rm NC}
+\alpha c\,\mathbf C_{\rm NC}
+\mathbf R_{\rm NC},
\qquad c\in[-1,1],
\]

with a scale-matched remainder and without a hidden RH-equivalent premise.

The target certificate is

\[
\boxed{
\operatorname{dist}
\left(0,\{\mathbf M_{\rm NC}+\alpha c\mathbf C_{\rm NC}:|c|\le1\}\right)
>
\rho_{\rm analytic}
+\rho_{\rm Polymath}
+\rho_{\rm bridge}.
}
\]

If this cannot be proved, Round 130 must identify the exact obstruction and must not promote a stronger claim.
