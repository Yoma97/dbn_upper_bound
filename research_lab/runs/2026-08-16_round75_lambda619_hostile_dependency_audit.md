# Round 75 — Hostile dependency audit of the `lambda >= 6.19` shoulder theorem

**Date:** 2026-08-16  
**RH status:** OPEN.  
**Truth status:** INTERNALLY_PROVED + independent 512/768-bit directed-rounding reruns.  
**Referee status:** PENDING.  
**Novelty:** NOVELTY_UNVERIFIED.  
**Role:** Removes the only material dependency concern found in a hostile audit of the new `lambda>=6.19` proof.

---

## 1. Audit question

The new reduction uses the published unconditional theorem

\[
\Lambda\le0.22
\]

to remove every time `t>0.22` from the multiple-zero problem, and then applies PSC on `0<t<=0.22`.

The logical reduction itself passed the circularity audit: if `t_0>Lambda`, a multiple real zero at `t_0` would, under a sufficiently small backward heat step, split according to

\[
e^{+\partial_w^2}w^m=i^{-m}Q_m(iw),
\]

where `Q_m=e^{-\partial_w^2}w^m` is the monic Hermite collision polynomial. For `m>=2` this creates a nonreal zero, contradicting real-rootedness at every time still strictly above `Lambda`. Thus every zero is simple for `t>Lambda`.

The endpoint `t=0.22` is not removed and remains explicitly inside the PSC rectangle.

However, the first version of the `6.19` proof joined its new direct rectangle

\[
0<t\le0.22,\qquad 6.19\le\lambda\le7.04
\]

to a historical `lambda>=7.04` shoulder layer. Because the project had subsequently designated the hostile-audited `lambda>=7.08` theorem as the robust canonical bridge, that dependency was unnecessarily ambiguous.

This round removes it completely.

---

## 2. Direct replacement cover

The committed K=2048 source

`research_lab/certificates/low_shoulder/convex_tail_psc_lambda619_mpfr.c`

has original SHA256

`2f3baa98f8c8e289fe6ee7aa270196c2fc4757a8230378c2a6ca861c4164f040`.

Its lambda grid consists of 0.01-wide rational boxes starting at `6.19`. The hostile audit performs the deterministic transformation

- `85` lambda boxes -> `89` lambda boxes;
- printed box count `85` -> `89` per time slice;
- diagnostic label `[6.19,7.04]` -> `[6.19,7.08]`.

No mathematical inequality, MPFR direction, finite-head rule, tail formula, phase bound, or error constant is changed.

The deterministic transformation is stored as

`research_lab/certificates/low_shoulder/extend_lambda619_to708.py`.

The generated verifier has SHA256

\[
\boxed{
376b2d065089da19c3f878c432fbf265e51d2567130ca729972835930b89196d.
}
\]

The same generated byte stream was used by all 512/768-bit jobs.

---

## 3. Exact domain

The time partition is the same exact rational 32-box partition used by the original K=2048 certificate:

- 18 boxes of width `0.01` on `[0,0.18]`;
- 6 boxes of width `0.005` on `[0.18,0.21]`;
- 4 boxes of width `0.002` on `[0.21,0.218]`;
- 4 boxes of width `0.0005` on `[0.218,0.22]`.

The lambda partition is now

\[
[6.19,7.08]
\]

with 89 exact rational boxes of width `0.01`.

Hence the direct hostile-audited rectangle contains exactly

\[
\boxed{32\times89=2848\text{ boxes}.}
\]

---

## 4. 512-bit monolithic rerun

GitHub Actions run `31924487091` compiled the generated source at 512 bits and evaluated all 2848 boxes in one run:

```text
PSC lambda [6.19,7.08] t<=0.22 PREC=512 ibox=[0,32) boxes=2848 fail=0
minimum_margin_lower=0.049218384939129022
worst_t=[0.219500,0.220000] lambda=[6.1900,6.2000]
A0_upper=0.82853202750245358 A1_upper=2.3383369555918039
GRID_RESULT pass=1
```

Thus every new box between `7.04` and `7.08` certifies as well; the weakest point remains the old lower-left terminal corner.

---

## 5. Independent 768-bit rerun

The identical generated source was compiled at 768 bits and run in four disjoint time-index chunks:

| time indices | boxes | failures | minimum lower margin |
|---|---:|---:|---:|
| `[0,8)` | 712 | 0 | `12.456560911890461` |
| `[8,16)` | 712 | 0 | `3.0034496597067775` |
| `[16,24)` | 712 | 0 | `0.30040464591597071` |
| `[24,32)` | 712 | 0 | `0.049218384939129022` |

The chunks are disjoint and exhaust all 32 time boxes and all 89 lambda boxes. Therefore they exhaust the same 2848-box rectangle as the 512-bit run.

The global weakest box and all displayed outward projections agree with the 512-bit run.

Canonical outputs:

- `research_lab/certificates/low_shoulder/convex_tail_psc_lambda619_to708_512.txt`;
- `research_lab/certificates/low_shoulder/convex_tail_psc_lambda619_to708_768_chunks.txt`.

---

## 6. Corrected dependency graph

The internally proved `lambda>=6.19` shoulder theorem should now be read using the following three pieces only:

### A. Direct finite/analytic rectangle

\[
0<t\le0.22,\qquad 6.19\le\lambda\le7.08.
\]

This is the K=2048 direct PSC cover certified above, together with the already audited Polymath error envelope. The endpoint `t=0.22` is included.

### B. Robust previously certified shoulder

\[
0<t\le0.22,\qquad \lambda\ge7.08.
\]

This is covered by the hostile-audited `lambda>=7.08` theorem (`2026-08-16_round73_lambda708_global_extension.md`).

### C. Interior real-rooted regime

\[
0.22<t\le\frac12,\qquad x\in\mathbb R.
\]

Since the published unconditional bound is `Lambda<=0.22`, every such time satisfies `t>Lambda`; the interior-simplicity lemma excludes multiple real zeros independently of lambda.

These three pieces overlap at their lambda boundary and leave no uncovered point in

\[
0<t\le1/2,\qquad \lambda\ge6.19.
\]

No historical `lambda>=7.04` result is needed anywhere in this corrected proof graph.

---

## 7. Circularity verdict

**PASS internally.**

The known input `Lambda<=0.22` is unconditional and strictly weaker than the desired eventual statement `Lambda<=0`. It is used only to discard times strictly greater than `0.22`; the boundary `t=0.22` is independently certified by PSC.

The finite rectangle uses only unconditional positive-time Polymath estimates, explicit phase/weight formulas, positive majorants, elementary convexity, Cauchy's estimate, and directed-rounding finite arithmetic.

Not used:

- RH;
- `Lambda<=0` or `Lambda=0`;
- real-rootedness at any `t<=0.22`;
- zero-spacing assumptions;
- pair correlation/GUE;
- generalized Laguerre positivity;
- Rodgers--Tao negative-time estimates derived under a `Lambda<0` contradiction setup.

---

## 8. Adjudicated status

Within the project proof system,

\[
\boxed{C_{\rm project-proved}=6.19}
\]

is now supported by a clean dependency chain that does not rely on the older provisional/historical `7.04` bridge.

This remains an internal computational/analytic theorem pending independent referee reconstruction. It does **not** prove RH, and novelty has not been verified.

The next quantitative bottleneck is the terminal line `t=0.22` at the lower lambda boundary. Any further lowering should be tested there first, while the deeper asymptotic structural frontier below the fixed-lambda triangle floor remains separate.
